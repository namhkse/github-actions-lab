# Plan CI/CD

Push --> Lint and Test --> Build --> Test --> Deploy --> Target

## Linting and Unit test

Linting: coding standard, improve code quality
Catch errors early in the design

Unit Tests: first tests run, checks code at theh component level
Fast runnin

## Build

Compile code into a machine readable format

Artifacts
binaries, archinves

Github package registry registri